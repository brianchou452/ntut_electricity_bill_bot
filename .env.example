# NTUT 電費帳單機器人 - 環境變數範例
# 複製此檔案為 .env 並填入實際值

# 必要配置
NTUT_USERNAME=your_student_id
NTUT_PASSWORD=your_password

# 啟動模式配置
BOT_MODE=api                 # 啟動模式: api (REST API 服務) 或 scheduler (定時爬蟲)
API_PORT=8000                # API 服務埠號（僅 api 模式使用）

# 調度配置（僅 scheduler 模式使用）
CRON_SCHEDULE=0 */1 * * *    # 每小時執行一次 (分 時 日 月 週)
RUN_ON_STARTUP=true          # 啟動時立即執行一次
TZ=Asia/Taipei              # 時區設定

# 資料庫配置
DB_PATH=data/electricity_bot.db

# Discord Webhook 通知配置
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_url
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 通知時間範圍配置
NOTIFICATION_START_TIME=06:00  # 開始發送成功通知的時間 (HH:MM 格式)
NOTIFICATION_END_TIME=23:00    # 停止發送成功通知的時間 (HH:MM 格式)
NOTIFICATION_BALANCE_THRESHOLD=100.0  # 餘額低於此數值才發送通知
